'use client';

interface ComponentSidebarProps {
  onAddComponent: (type: string, index?: number) => void;
}

export default function ComponentSidebar(props: ComponentSidebarProps) {
  return null;
} 